{
  "name": "root",
  "root": true,
  "private": true,
  "license": "MIT",
  "scripts": {
    "format": "lerna run format",
    "play:dev": "lerna run play:dev",
    "play:deploy": "lerna run play:deploy",
    "play:preview": "lerna run play:preview",
    "build": "lerna run build",
    "build:prod": "lerna run build:prod",
    "test": "lerna run test",
    "node:script": "lerna run build:prod && lerna run node:script",
    "demo": "lerna run build:prod && lerna run demo",
    "patch": "generi log patch",
    "minor": "generi log minor",
    "major": "generi log major"
  },
  "devDependencies": {
    "generi": "^1.2.1",
    "lerna": "6.6.2"
  },
  "packageManager": "pnpm@8.5.0"
}